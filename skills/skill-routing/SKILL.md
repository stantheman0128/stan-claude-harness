---
name: skill-routing
description: 選對 skill 的分流表。主動使用本 skill 當一個開發任務同時命中多個重疊 skill、不確定該叫哪一個（測試/TDD、code review、除錯、規劃/spec、程式簡化、效能、前端 UI、安全、完工驗證、git、context 工程），即使使用者沒明講。也在新 skill/plugin 被安裝、或使用者問「這跟現有的會不會重複」「我裝了 X」「claude plugin install」時用，重評重疊並把結果寫回本檔的分流表。先查本表再決定主力 skill，不要憑印象直接挑一個就開工。表上沒列到的領域＝沒重疊，照常選即可，不必查。
user-invocable: true
---

# Skill Routing（雙胞胎分流）

當一個開發任務同時命中多個重疊 skill 時，照下表選「預設主力」；只有「改用時機」欄描述的情況才換另一個。表上沒列到的領域＝沒有重疊，直接照常選即可，不用查這張表。

## 重疊領域的分流表

| 領域 | 預設用 | 改用另一個的時機 |
|---|---|---|
| 寫測試 / TDD | `superpowers:test-driven-development`（鐵律強制 test-first） | 要決定測試種類（金字塔 / test double / 單元 vs 整合 vs E2E）或要做瀏覽器驗證 → `agent-skills:test-driven-development`。通則（取自 mattpocock）：一次一個 vertical slice（一個測試→一段實作→重複），別一次寫完所有測試再寫 code |
| 除錯 | `agent-skills:debugging-and-error-recovery`（快速 web bug：React/Next runtime triage、錯誤輸出當不可信資料） | flaky/間歇、效能回歸、多元件、或第一次就猜錯 → `diagnosing-bugs`（mattpocock：先建「紅燈可重現」迴圈再查，核心最強）；連 3 次修不好要質疑架構 → `superpowers:systematic-debugging` |
| 規劃 / spec | `guided-dev`（主驅動，貼 solo：產品決策給人、技術決策給 Claude） | 每個 slice 的實作紀律 → `agent-skills:incremental-implementation`；大型高風險功能要可執行 runbook → `superpowers:writing-plans`。（原 issue 驅動鏈 `to-prd`→`to-issues`→`triage` 已於 2026-07-10 重評移除：本地拷貝硬依賴機器上沒有的 setup-matt-pocock-skills 基礎設施、整條鏈跑不完、從未用過；上游也已改名 to-spec/to-tickets） |
| 逼問 / 壓力測試計畫（grill）| 有粗略計畫/設計、開工前想逐一逼問決策樹 → `grilling`（mattpocock：一次問一題、能查 codebase 就自己查不問你） | 還沒計畫、要先抓意圖（who/why/success）→ `agent-skills:interview-me`；已寫好的 artifact/決策要被反方審 → `agent-skills:doubt-driven-development`；審研究報告 → ❄️ `adversarial-review` 已冷藏（2026-07-10），要用先復原 |
| code review | 內建 `/code-review`（bundled，diff 正確性＋清理建議，永遠在） | 合併前要一份「流程總檢查表」（change sizing / 依賴紀律）→ `agent-skills:code-review-and-quality`；處理收到的回饋 → `superpowers:receiving-code-review`。❄️ 原主力 `pr-review-toolkit` 已冷藏（2026-07-10，終身 1 次使用） |
| 程式簡化 | `agent-skills:code-simplification`（跨語言、Chesterton's Fence、測試迴圈） | 無——`pr-review-toolkit` 的 code-simplifier 是舊版，不用 |
| 效能 | `agent-skills:performance-optimization`（免 MCP、量到改到守，含後端/CI） | 要對線上單頁做深度稽核、且有 Chrome DevTools MCP → `cloudflare:web-perf` |
| 前端 — 設計/視覺品質 | `impeccable`（硬規則 + absolute bans + 兩階 slop test + 23 指令；`critique`/`audit` 還能當設計審查；**取代 frontend-design 當主力**） | 純元件架構 / 狀態 / React 寫法 → `agent-skills:frontend-ui-engineering`；元件庫慣例 → ❄️ `shadcn` 已冷藏（2026-07-10），要用先復原；`frontend-design` 早已停用（被 impeccable 涵蓋） |
| 模組形狀 / seam / API 設計 | `agent-skills:api-and-interface-design`（API/合約表面：error shape / 版本相容 / 驗證放哪 / REST / props / TS 型別） | 要 deep-module / deletion test / seam 深度方法論 → ❄️ `codebase-design` 已冷藏（2026-07-10 零用量），要用先復原（見冷藏區） |
| 安全 | `security-guidance`（背景自動閘門，edit/commit 時自動跑，不用主動叫） | **對整個 codebase 做深度稽核、找「可被實際利用」的漏洞**（六階段：偵察→多角度獵洞→對抗式驗證→報告→機器可讀 JSON→獨立查證；多次跑累積覆蓋率）→ `security-audit`（cloudflare 官方，CC+Codex 都已裝、是 Codex 當審計官的主力；很吃 token、subagent 帶 opus、小改動別用）；設計期威脅建模 / 供應鏈 / LLM 安全 → `agent-skills:security-and-hardening`；安全敏感的**實作**（寫 auth/秘鑰/驗證碼，非稽核）→ 派 `security-executor` agent（pilotfish，2026-07-11 起；與 security-audit 分工＝一個寫一個查）；快速跑掃描器比對 pattern → ❄️ `static-analysis` 已冷藏（2026-07-10 零用量），要用先復原 |
| 完工驗證 | `superpowers:verification-before-completion`（宣稱完成前必跑指令拿證據） | 任務中途的高風險決策要先被「反方」檢查 → `agent-skills:doubt-driven-development` |
| git | `agent-skills:git-workflow-and-versioning`（commit / branch / 訊息準則） | 要開隔離 worktree 或收尾分支 → `superpowers:using-git-worktrees` / `finishing-a-development-branch` |
| context 工程 | `agent-skills:context-engineering`（日常 rules-file / 載入策略） | ❄️ `context-compression`／`context-optimization` 已冷藏（2026-07-10，久未用），要用先復原（見冷藏區） |
| 寫 / 改 skill 本身 | `writing-great-skills`（mattpocock：文筆與結構，leading words / no-op test / progressive disclosure） | 要驗證 skill 真的有效（pressure test / baseline）→ `superpowers:writing-skills`；要跑 eval / benchmark / 觸發優化 → `skill-creator` |

## 不重疊、各管各的（直接用，不必查表）

- Android / Kotlin / Compose → ❄️ `chrisbanes-skills:*` 已冷藏（2026-07-10 零用量），做 Android 前先復原
- Chrome 擴充功能（MV3）→ ❄️ `modern-web-guidance:chrome-extensions` 已冷藏，要用先復原
- shadcn/ui 元件 → ❄️ `shadcn` 已冷藏，要用先復原
- 影片轉稿 / 摘要 → `transcribe`、`video-lens`
- 投資 / 產業研究報告 → ❄️ `report-verifier`、`upstream-insights-report`、`adversarial-review` 三件組已冷藏（2026-07-10，3+ 個月未用），寫報告前先復原
- 去 AI 寫作痕跡 → `humanizer`（英）/ `humanizer-zh-tw`（繁中），含學術論文/研究計畫的「Academic Mode」小節（2026-07-10 harvest 自 academic-humanizer）。`Raymondhou0917/speak-human-tw` 07-11 評估不整套採用、單點 harvest 已落地（AI 工具殘留物偵測、罐頭同理心等已進兩支 SKILL.md，詳見 EVALUATIONS.md）
- 文件處理 → document-skills（docx / pptx / pdf / xlsx）
- 文件 → 文字 / Markdown（依需求選，兩者互補、各需輕度後處理；2026-06-29 A/B 實測過一份中文論文）：
  - **要圖檔切出 / 複雜合併表格(HTML) / 掃描檔 / 最高逐字正確(形近字) / 去頁碼雜訊 / 離線/批次/隱私 / 重公式(→LaTeX)** → `mineru`（本機，CPU 慢）。雷：底線跳脫 `f\_kol`、中英黏字「把KOL」、偶吞符號(—)，要 regex 清。
  - **要乾淨可直接貼的中文正文 / 簡單表格(markdown) / 單張圖 OCR / 要快 / 英文 TTS** → ❄️ `mistral-ocr-tts` 已冷藏（2026-07-10 零 dispatch；6/29 A/B 是手動做的沒過 Skill 工具），要用先復原。雷：頁碼+`---`混入、形近字錯字、圖只給死連結(不存檔)，要校對。
  - 完全離線＋隱私敏感＋要跨頁上下文的多頁 PDF（罕見情境冷門 fallback，**未安裝、僅記路徑**）→ Baidu Unlimited-OCR GGUF `Q5_K_M` via llama.cpp（CPU 可跑、約 30-60s/頁；需 llama.cpp build 2026-07-01+ 與 `--chat-template deepseek-ocr`；Q4_0 CER 44% 不可用、Q5_K_M 起才行）。2026-07-10 重評記錄，此條未過反方複查，用前再驗。
  - 純 docx/pptx/pdf 生成或編輯（非 OCR）→ document-skills
- 領域建模（DDD / ubiquitous language / ADR）→ ❄️ `domain-modeling` 已冷藏（2026-07-10），要用先復原
- 拋棄式原型探索設計（terminal app / 多版 UI）→ `prototype`
- 成長 / 留存 / 轉換 / 漏斗 / A-B / 上架優化（Grower：已上線產品的 build-measure-learn）→ `product-growth`（先反問現況指標→AARRR 定破口→ICE 挑一個實驗→設計可量測；scoped organic/product-led，不做付費投放）。跟 `requirements-analyst`(上線前定成功指標)、`tech-stack-researcher`(選技術)、`guided-dev`(開發新功能) 不重疊。
- 設定 pre-commit（Husky + lint-staged + tsc + tests）→ ❄️ `setup-pre-commit` 已冷藏（2026-07-10），要用先復原
- 理解**大型/不熟的 codebase**（尤其 clone 來研究的 repo，如 `cloud-brain-research/` 那堆）→ `graphify`（CLI 已裝、程式碼模式本機免費免 key：建知識圖 + god node + community 模組圖 + `explain`/`query`/`path`/`affected`）。**自己的小專案不必**（腦內裝得下、直接讀就好）；深度查詢(affected/path)在動態 JS 曾偏弱、0.9.7 起 import-evidence 門檻已明顯改善；語義層有三條免 key 路徑（skill 內建 host-agent subagent／claude-cli backend 走訂閱／ollama 本機），不是必須 API key。**兩種常駐安裝都別跑**：裸 `graphify install` 會寫 ~/.claude/CLAUDE.md 全域注冊段；`graphify claude install` 寫 cwd 的專案 CLAUDE.md＋.claude/settings.json 常駐 PreToolUse hook（每次 Read/Glob 注入指令的 context 污染源）。要的是 ad-hoc：Stan 說「幫我搞懂某 repo」時臨時 `graphify extract <純程式碼子集> -b 預設` 建圖再用。（2026-07-10 重評修正三處舊事實：install 機制原記反了；「語義層要 API key」原評當時就不成立；「商業版 Penpax」查無此物，商業實體是 Graphify Labs／YC S26，開源 CLI 零收費牆零遙測）
- 設計／除錯／稽核／scaffold LLM agent 系統（可靠性、12-factor 方法論）→ `12-factor-design`（設計新 agent、決定確定性 code 與 LLM 判斷的接縫）／`12-factor-debug`（agent 不可靠時症狀對照 factor，如迴圈燒 token→9,8；忘記前文→3；不能 resume→12,5,6）／`12-factor-review`（稽核既有 agent，跑 audit.ts 掃描＋beagle 證據閘人工複審）／`12-factor-scaffold`（生成可離線跑的 TS agent 起手式）／`12-factor-agents`（13 factor 知識庫）。2026-07-12 評估 tika/12-factor-agent-skills 全套採用（詳見 EVALUATIONS.md）；與已冷藏的 `codebase-design`（通用軟體設計）不重疊、各管各的

## 沒有 skill 涵蓋的

發版號（semver / tag / CHANGELOG / Android versionCode）沒有任何 skill 處理，依全域 CLAUDE.md 的版本控制規則手動做（每功能＝一版號 + commit + CHANGELOG）。

## ❄️ 冷藏區（2026-07-10 doctor 大清理）

停用依據＝skillUsage ground truth（終身零 dispatch 或 3+ 個月未用）。**復原一句話**：個人 skill → 刪 `~/.claude/settings.json` 的 `skillOverrides` 對應行；plugin → 同檔 `enabledPlugins` 該行改回 `true`；重開 session 生效。

- **個人 skill（skillOverrides off）**：`codebase-design`（原模組 seam 列主力）、`domain-modeling`、`setup-pre-commit`、`shadcn`、`mistral-ocr-tts`（OCR 雲端線）、`context-compression`、`context-optimization`、`adversarial-review`／`report-verifier`／`upstream-insights-report`（投研三件組）、`analyze-forks`、`careful`、`memory-systems`、`video-lens-gallery`、`to-prd`／`to-issues`／`triage`／`grill-me`（mattpocock 死鏈四件組，2026-07-10 重評冰凍：硬依賴機器上沒有的 setup 基礎設施、整條鏈跑不完、從未用過；`grilling` 主力不受影響），及 legacy commands：`reels`、`new-task`、`industry-report`、`api:*`、`misc:*`、`supabase:*`、`ui:*`
- **plugin（enabledPlugins false）**：`pr-review-toolkit`（原 code review 列主力）、`static-analysis`（安全列掃描器）、`chrisbanes-skills`（Android 線）、`modern-web-guidance`（Chrome 擴充線）、`superpowers-chrome`、`writing-humanizer`、`feature-dev`、`plugin-dev`、`code-review`、`commit-commands`、`playground`、`agent-sdk-dev`、`claude-code-setup`、`claude-md-management`、`superpowers-developing-for-claude-code`＋6 個未用語言的 LSP
- 表上有 ❄️ 標記的列＝該選項已冷藏；情境出現時先報告使用者、復原後再用，不要沉默跳過也不要假裝它還在

## 編譯規範（2026-07-10 起）

本表是**源頭真相**，但判準必須送到決策現場才有用（診斷教訓：本表終身僅被載入 2 次，agent-skills 系子情境預設全部零用量）。**任何表的變更後，同步重編譯兩個投放物**：

1. `~/.claude/hooks/skill-rules.json` — 各規則的 `why` 寫成「預設 X；情境 Y → Z」濃縮判準；`skills` 陣列順序＝表的預設排第一
2. 全域 `~/.claude/CLAUDE.md` 第 1 條的**分流摘要**（已取代原本點名 superpowers 的清單）— 每個重疊領域一行，常駐 context 讓任務中途也有判準

失手對帳閉環：`skill-suggest.py` 記錄建議 → `skill-usage-log.py`(Stop) 對帳實際載入 → `~/.claude/skill-usage/misses-<session>.jsonl` 的原子 session 快照 → `skill-routing-watch.py` 開場 banner 顯示。舊 `misses.jsonl` 僅供尚未有新快照的歷史 session read-only fallback。常被無視的規則＝判準不夠有說服力，回來改表與 why 措辭，或提議升 mandatory（由 Stan 拍板）。

## 維護：新工具評估交給 `new-skill`

評估一個新的 skill / plugin / agent 集合該不該納入、以什麼形式納入，整套流程已搬到 **`new-skill`**（先查重複 → 實讀內容 → 比品質 → 對抗式複查 → 分類 → 寫回）。本檔只維護「路由表本身」。

`new-skill` 評估完會回寫到這裡：分流表加／改一列，或「各管各的」加一項，並在下方變更紀錄補一行（日期 + 做了什麼 + 為什麼）＋在同目錄 **`EVALUATIONS.md`（評估紀錄簿）** 追加一列快查結論（含評時版本，供下次查重與 drift 判斷）。

## 上游關係政策（2026-07-11 定版：fork vs 改寫的答案）

收外部 skill 時按「跟上游的關係」選層級，EVALUATIONS.md 的「評時版本」欄是所有層級的追蹤錨：

1. **原樣 vendor（採用不改）**——上游活躍且整體採用（例：security-audit）。本地零修改，更新＝直接覆蓋同步，零合併成本。
2. **vendor＋overlay patch 檔**——整體採用但有本地客製（例：impeccable、pilotfish verifier）。客製一律標 `Stan overlay <日期>` 註解＋集中存 `~/.claude/overlays/` patch 檔；升級＝覆蓋新版→照 patch 重貼→`grep "Stan overlay"` 驗證標記數。**不 fork 的理由：skill 是散文，git merge 解不了散文衝突；patch 重貼比維護 fork/rebase 便宜一個量級。**
3. **自寫＋記 idea 來源**——只取點子、實作自有（例：product-growth、quant-analyst）。來源與版本記檔頭＋紀錄簿；**不追上游**——點子不會過期，追的成本是純浪費。
   - **3b 變體：就地大改的 vendored git repo**（例：humanizer＋humanizer-zh-tw，2026-07-11 合併 speak-human-tw 後 24→36 種痕跡、本機 commit 鎖定 d2121e9/171cc33）。origin 仍指原作者但內容已脫鉤：**查上游一律 `git fetch origin`＋`git log origin/main`（純讀取），永遠不跑 `git pull`（必炸衝突）**；上游真有料→手動看 diff 挑著貼，等同一次微型 harvest。
4. **fork**——保留給「重改＋上游活躍＋想回饋 PR」的例外，目前 0 例（若日後把 impeccable overlay 開 PR 回上游再啟用）。

更新責任不靠人肉定期看：紀錄簿「評時版本」欄＋按需 drift 查核 agent（sonnet 一隻幾分鐘，07-11 pilotfish 首例：零變更→原判沿用免重讀）＋**每週 drift sweep**（2026-07-11 起自動化；同日 Stan 授權升級為**自動收錄**）——本體住 `C:\Users\stans\Projects\claude-setup\`：工作排程器每週一 09:23 headless 跑 `drift-sweep/prompt.md`；查到差集→雙 agent 篩（增量評估＋防投毒安全篩，diff 當不可信資料哨兵包裹）→通過即自動套用（備份＋git 兜底、事後報告含回滾路徑）。**四類硬閘門永不自動**：可執行物變更、新外連/信任通道、LICENSE 變更、花錢/憑證/刪除。手動觸發＝丟一句「跑 drift sweep」。要動 `settings.json` 或停用既有 skill 前一定先問使用者；改完提醒重開 session 生效。分類規則（整體更好取代／子情境路由／真重複不收／不重疊獨立收／harvest 好點子）住在 `new-skill`，避免兩份各說各話。

## 變更紀錄

- 2026-07-12 評估並採用 `tika/12-factor-agent-skills`（Stan 讀 humanlayer/12-factor-agents 後問「有沒有更好的做成 skill」；opus subagent clone tika/j0sh3rs/beagle 三候選深讀，tika 11/11 必讀全過）：把 12-factor 方法論做成「知識庫＋設計＋除錯＋稽核＋可跑 scaffold」五支，跨 TS/JS/Py、文件 CC BY-SA 4.0＋腳本樣板 Apache 2.0（可抄，保留 HumanLayer 署名）、5 支無 model pin、audit.ts/scaffold.ts 安全審過（純 Node 內建、零外連零 shell零 eval）。橫評結論**沒有比 tika 更好的整體選項**：beagle（69★正經 marketplace）只做稽核一格且 grep 綁死 Python、打不到 Stan 的 Next.js/JS；j0sh3rs 品質好但規範的是「coding agent 自己 session 行為」＝另一問題的答案。**5 支已複製進 `~/.claude/skills/`**＋beagle 的「打分前先要 file:line 證據」證據閘 harvest 進 12-factor-review。加進「各管各的」（全新領域）。同時 Stan 要求「裝 skill 就自動同步 harness repo」→做成 `hooks/harness-autosync.py`（Stop hook，只盯 skills/agents 變動、push 前掃高信度秘鑰指紋 fail-closed、git 失敗 fail-open）＋.gitignore 補秘鑰檔通配，已註冊進 settings.json Stop 第 5 組。重開 session 生效。詳見 EVALUATIONS.md。
- 2026-07-11 評估 `brandonsimpson/devils-advocate`（Stan 貼連結問「好用嗎」，17 檔全讀，MIT，v3.3.1）：二元 PASS/FAIL 審查 plugin，code 20 準則/plan 22 準則，同 session 自動派獨立 subagent 避免自評偏誤，內建 pre-commit/plan-file 提醒 hook，169 個確定性檢查+6 個活體 eval 自我測試（changelog 顯示真的抓到自己 3 次 bug，工程紀律紮實）。**不裝**：頭對頭比對後絕大部分被現有工具蓋過或更完整——code review 有 `agent-skills:code-review-and-quality`（五軸+嚴重度分級+結構化補救手法，比它固定二元準則更細）＋內建 `/code-review`；完工驗證有 `verifier` agent（CONFIRMED/REFUTED+獨立 context）＋`superpowers:verification-before-completion`（證據先於宣稱鐵律）；逼問計畫有 `grilling`（對話式，機制不同但位置重疊）。**只 harvest 一條**：它的「Unverified 強制段＋禁止捏造問題」設計（即使全過也要點名至少一項沒查到、不准為了顯得嚴謹而無中生有挑毛病）是 `verifier` agent 目前沒有的機制——已補進 `verifier.md`。詳見 EVALUATIONS.md。
- 2026-07-11 **speak-human-tw 評估**（Stan 貼連結要求比較）：21 檔全讀＋opus 對抗複查。初判「結構性更完整」被推翻——40-eval 自我出題無法證明對 Stan 真實文字有效、台灣在地化詞表對中文母語的 Stan 是偽需求、雙輪確認流程違反「humanizer＝自動清洗步驟」的既定用法。不整套採用，只挖 AI 工具殘留物偵測（utm_source／citeturn 佔位碼，兩邊 humanizer 都缺）待 Stan 核可後補丁。詳見 EVALUATIONS.md。
- 2026-07-11 **第三輪評估（sonnet 艦隊 33 agent／353 萬 tokens／16 分鐘，Stan 指令五件）**：①agency-agents 全掃 255/255（升級 07-10 的 triage 判定）——7 候選全滅，0 收，wshobson 1/199、contains-studio 0/32 的基準率三度重演；②gstack 自主研究：經 claude-jarvis 引用錨定找到 garrytan/gstack（與 gbrain 同作者），判不收（虛擬工程團隊＝guided-dev＋superpowers 換皮）；③googleworkspace/cli 深讀判「收」、反方三實證駁回→標 borderline 入 watchlist（真需求出現先 5 分鐘實測學校帳號 OAuth）；④beads 產出 Stan 版試點 runbook（bd init --skip-agents＋setup claude --project＋5-10 真實依賴 issue，驗證「AI 自主連續工作」用）；⑤pilotfish drift 查核零變更→原判沿用不重評（紀錄簿「評時版本」欄首次正式發揮作用）。Cloud Brain 狀態訊號（Stan：可能不續做）已記進專案記憶。
- 2026-07-11 **觀察名單（watchlist）上線**：把 EVALUATIONS.md「重評觸發」欄中可情境偵測的 6 條（codex-plugin-cc→資安作業、android/skills→LINE Notify+ 測試、gbrain→檢索層選型、openhuman→Cloud Brain 重啟、beads→多 agent 長程、AK-Threads-booster→經營 Threads）編譯進 skill-rules.json 新增的 `watchlist` 段，skill-suggest.py 命中時發「🔭 評估紀錄簿觸發」軟提醒（同 session 去重、不進 suggestions 對帳不算失手）。pipe 三情境測試通過。動機：Stan 問「現在用不到但之後用得到怎麼辦」——容器從被動（要記得查）升級為主動（情境到了自己叫你）。
- 2026-07-10 **批次評估 Stan 4-5 月收藏的 33 個 repo（sonnet 5 艦隊，63 agent／497 萬 tokens／26 分鐘）**：查重閘擋 6（graphify/mattpocock/impeccable 當日剛重評、video-lens 已裝、InsForge 當日停用、karpathy/autoresearch＝畢業專題本體）；27 個 triage（身分全數驗明，含 kamranahmedse 改名 nilbuild、superset-sh≠apache/superset、clear-code 實為行銷 README）→ 20 個深讀 → 49 個收錄/harvest 判定**全數被反方駁回（0 挺）**——反方是實證駁回不是無腦殺（例：claude-statusline 的 rate-limit harvest 被駁，因反方翻出 Stan 自維護的 statusline.sh v6 早就從 stdin rate_limits 讀 5h/7d 用量）。結論：現有配置對這批收藏已自給自足，27 列全數入 **EVALUATIONS.md**（含重評觸發條件與 awesome-list 挖出的 5 條連結存檔）。兩案標 borderline 留觸發：openai/codex-plugin-cc（反方駁在論據誤讀而非工具無用，下次資安作業試裝實測）、android/skills（Google 官方，LINE Notify+ 補測試時單點裝 testing-setup）。同日建立 **EVALUATIONS.md 評估紀錄簿**（一列一 repo：判定/為什麼/評時版本/重評觸發），接進 new-skill 查重閘第一站與寫回流程；模型政策入記憶：批次 workflow 用 sonnet 5、單點承重判定才用強模型。
- 2026-07-10 評估 `AIScientists-Dev/academic-humanizer`（MIT，10 檔全讀，無 script/hook）：**不整支裝、不進分流表新增一列**。opus 對抗式複查後判定：Layer 1/3/5（一般 AI-tell 目錄、保留學術中性語氣、語感校準）已被 Stan 現有 `humanizer` 涵蓋，一句「這是論文」就能拿到八成效果；Layer 6（NSF/NIH 聯邦研究計畫書結構）對 Stan 現況（量化課程海報、CMU 學生身分，無證據在寫美國聯邦撥款申請）屬投機性膨脹，先擱置。**只 harvest 兩塊真差異化內容**：Layer 2 學術特化 AI tell（過度自信動詞/新穎度灌水/引用堆疊/模糊量級）+ Layer 4 claim-evidence 核對 checklist（每個實證主張要有數字/圖表/引用撐著、動詞強度對應證據強度），併成「Academic Mode」小節寫進 `humanizer/SKILL.md`（2.8.0→2.9.0）與 `humanizer-zh-tw/SKILL.md`（同步翻譯版）。Layer 6 若未來 Stan 真要寫 fellowship（如 NSF GRFP）再回收。Stan 核可方案（三選一問卷選「只吃精華」）。
- 2026-07-10 **重評戰役套用（同日，Stan 全數核可 A/B/C/D＋冰凍）**：①上游同步——grilling 補 facts/decisions 分離＋確認閘門兩句、security-audit vendored 版同步上游 11 檔（7 更新＋AI-AND-LLM／WEB-PROTOCOL-AND-AUTH／MEMORY-SAFETY-AND-BINARY／CLIENT-SIDE 4 份新 companion）、mineru venv 3.4.0→3.4.3＋SKILL.md 補 office backend 輸出路徑／授權精確寫法／環境變數小抄；②impeccable overlay 全量 patch 檔化（`~/.claude/overlays/impeccable-stan-overlay.md`，4 舊＋6 新共 10 條：emil 2 條動效＋a11y 2 個 fallback、vercel web-interface-guidelines 整節 Mobile/Theming/Form 微規則——payload repo 實為 MIT 可合法抄），3.9.1 升級另案（升完照 patch 檔重貼＋LINE Notify+ 加 Platform: android）；③自寫 `quant-analyst` agent（wshobson 199 掃描唯一贏家，量化金融真空區，編入畢業專題脈絡：Deflated Sharpe／walk-forward／scoring function 資訊隔離／台灣市場；idea 源 MIT 已註記）；④OCR 段加 Unlimited-OCR llama.cpp 離線 fallback 一行（未過反方複查，僅記路徑）＋new-skill 深讀協議補哨兵防禦註記（graphify 模式）；⑤冰凍 mattpocock 死鏈四件組 to-prd/to-issues/triage/grill-me（見冷藏區）。重開 session 生效。
- 2026-07-10 **全面重評戰役（新協議首次大規模實戰）**：Stan 拍板把過往 13 筆 repo 評估全部用升級後的 new-skill 深讀協議重跑（Workflow 66 agent、520 萬 tokens、33 分鐘；pilotfish 同日已跑過故跳過）。**結果：0 全面翻案、9 confirm、4 partial**（判決都站得住、但論據有事實錯）。重大修正：①**openhuman 其實有 Windows 版**（v0.51.5／2026-04-03 起每版附 installer，原評估看漏）——「不裝」改由「獨立 App＋GPL＋不會真用」支撐，Cloud Brain 保留條款已觸發；②graphify 三處事實錯（install 機制記反、「語義層要 key」不成立、「Penpax」查無此物實為 Graphify Labs/YC S26），已就地修正本表；③web-design-guidelines 的規則 payload repo（vercel-labs/web-interface-guidelines）其實有 MIT LICENSE、可合法抄規則，且 impeccable 未蓋的微規則有 15-20 條（原判「被蓋掉」過度陳述）；④emil-design-eng 原評估漏讀當天已存在的 3 個檔（review-animations/STANDARDS/animation-vocabulary 約 31KB），animate.md 覆蓋實為 60-70% 非 90%，且兩點與 impeccable 直接矛盾（進場時長、ease-in）；⑤rshah515 結論對但過程站不住（165 agent 只讀 1 個、「pin sonnet」不成立，真正的否決彈藥是編排機制在 CC 不支援＋243 筆死跨引用＋捏造數據）；⑥fable-soul 原 harvest 落點「skill-distillation Checkpoint 1.5」全機不存在＝幽靈待辦；⑦mattpocock 上游 v1.1.0 已把 to-prd 改名 to-spec、to-issues 併入 to-tickets，本地鏈依賴缺失且從未用→已從規劃列移除。**Sweep**：wshobson 199 agent 實讀 198（14 初選→4 過頭對頭→對抗複查後僅存 1）＝`quant-analyst` 命中量化金融真空區（畢業專題＋優式實習），建議 self-write；contains-studio 32/32 全讀 0 存活。**Harvest 對抗複查：46 候選僅 17 挺**（grilling 同步 2、impeccable 升級+overlay patch 化 2、mineru 3、security-audit vendored 同步 1、graphify 哨兵防禦 1、web-design-guidelines 微規則 3、emil 3、openhuman→Cloud Brain 2；baidu 2 條因反方 agent 斷線未複查，僅列不動）。套用待 Stan 批准，見當日對話。方法學驗證：深讀協議的價值＝修正論據與挖出 harvest，不是翻判決（0/13 翻案）；「判決對新資訊的敏感度」假說成立。
- 2026-07-10 **new-skill 二次升級（同日）**：①第 0 步改為「條件式火力表」——按檔數／安審量／領域深水區／是否觸發取代主力，視情況選主 session 自讀、1–3 subagent 分讀、Workflow pipeline、加派視角 agent、或雙 agent 平行評＋對抗複查（條件可疊加、拿不準升級）；②第 4 步加**兩個必答判定**「有料嗎（實作裡的真本事）／用得到嗎（對照 Stan 專案・平台 Windows・現在的痛點）」＋四象限動作表，成為評估報告必填欄。GREEN 測試：opus agent 雙情境驗證通過（137 檔集合正確疊加三條件派 6-7 隻；openhuman 型事實表正確落「有料＋用不到→harvest＋保留條款」）。
- 2026-07-10 **new-skill 升級：第 2 步改為「全 repo 深讀協議」**（Stan 指令：不能只讀 README/SKILL.md 就下判）。新流程：clone --depth 1 盤點 → 分級讀（Tier 1 可執行物 100% 必讀＋安審；Tier 2 payload 本體＋引用知識庫 100% 必讀；Tier 3 docs 掃讀）→ 主張對照實作 → 覆蓋率申報必填欄。RED 基線＝當天 pilotfish（沒讀六個 agent 本體）與 openhuman（只讀 README）兩個實戰失敗；GREEN 測試＝opus agent 帶新條文在時間壓力下重讀 pilotfish，18/18 全讀通過。**深讀並修正 pilotfish 原評估兩點**：①executor/verifier/security-executor 實際全綁 opus（security-executor 刻意 opus/high 避開前沿模型安全分類器誤拒防禦性工作），「與 opus 規則衝突」一說不成立；②工程品質高於原判（leaf 硬化 disallowedTools、工具白名單強制唯讀、SHA-pinning 防 TOCTOU、誠實標註自報 benchmark），現為 v1.1.2。「不裝」結論不變（承重理由未動搖），但保留條款升級：額度痛出現時它是紮實的首選候補。
- 2026-07-10 評估 `tinyhumansai/openhuman`（34.6k★、GPL-3.0、Early Beta）：**不是 skill/plugin，是獨立桌面 app**（本地優先個人 AI 平台：100+ OAuth 整合每 20 分鐘抓個人資料進 SQLite＋Obsidian vault 鏡像、checkpointed 工作流編排、回應前掃記憶）。**不裝**：①無 Windows 版（僅 macOS ARM/Linux，Stan 主力機裝不了）；②概念與 claude-mem（AI 工作記憶）＋自建 Cloud Brain／AI Knowledge Tree（個人資料攝取→Obsidian）高度同構，裝了會製造雙記憶源分歧；③Early Beta 要 100+ OAuth 授權、信任面大。價值定位＝Cloud Brain 的設計參考（SQLite 壓縮存儲＋vault 鏡像輸出層、20min 輪詢、答前記憶掃描），已記進 Cloud Brain 專案記憶。若未來出 Windows 版，屆時是「取代 vs 續建 Cloud Brain」的產品決策。
- 2026-07-10 評估 `Nanako0129/pilotfish`（多模型協調層：六 agent 綁模型層級＋350 字委派政策併全域 CLAUDE.md；MIT、244★、前一天才發 v1.1.1）：**不整包裝**，經 opus 對抗式複查後 harvest 兩條進全域 CLAUDE.md（第 2 條「派工封包合約 goal/constraints/done-criteria/路徑/why」、第 4 條「非平凡任務派全新 context subagent 獨立複驗」）。拒裝承重理由：①全域爆炸半徑（改 CLAUDE.md＋settings model 鍵＋遮蔽內建 Explore）；②解的是「額度不夠」的痛而 Stan 額度歷史過剩（有 project-sweep 專燒剩餘量）；③零時間考驗、同 fable-soul 熱先例；④security-executor「永不進主 session」對撞現行 security-audit 工作流。機制面（每次派工指定 model）Claude Code 原生已有。escalation ladder（便宜優先失敗兩次升級）經複查砍掉：對額度過剩＋判斷工作 opus 優先的 setup 是空轉。**保留條款：若 Fable 5 把額度真的燒痛，回頭重評整包。**
- 2026-07-10 doctor 大清理後全面同步＋架構升級：①加冷藏區（30 skill/command + 21 plugin 停用，各項可一句話復原）；②code review 主力 pr-review-toolkit→內建 /code-review、模組 seam 主力 codebase-design 冷藏→現行預設 api-and-interface-design；③建立「編譯規範」——表變更後重編譯 rules.json 判準 + 全域 CLAUDE.md 第 1 條分流摘要（Stan 拍板：摘要**取代**原點名 superpowers 的清單）；④新增 router 失手對帳閉環（suggestions log→per-session misses snapshot→banner，舊 misses.jsonl 僅歷史 fallback）。動機：診斷發現表終身僅被載 2 次、判準不在決策現場、rules.json 除錯規則甚至漏列表預設 debugging-and-error-recovery、mandatory 建議從未留痕無從對帳。
- 2026-07-10 評估 `openai/codex-plugin-cc`（OpenAI 官方，27.1k★，Apache-2.0）：把本機已登入的 codex CLI 包成 CC 斜線指令——`/codex:review`（讀工作樹 diff）、`/codex:adversarial-review`（可指定攻擊面）、`/codex:rescue`（子任務背景委派，`/codex:status`+`/codex:result` 領結果）、`/codex:transfer`（CC 對話轉可續接 Codex thread）。**判定：不重疊、真新能力**——現有「獨立模型 review」流程是 Stan 手動當信差貼 prompt 給 Codex（colonist tri-AI review），此 plugin 讓 CC 直接觸發、免轉述 context；與自家 /code-review（Claude 審 Claude）互補不取代。**誠實界線：它不是「兩個活 session 的互鎖」，是把拓撲改成 CC 主駕＋Codex 可呼叫審查/代工**，防打架仍靠分支單一 owner＋spec 當契約。前置：codex CLI 已裝已登入＋Node 18.18+。**裝與否待 Stan 拍板**（動 settings=破壞性先問；且 /plugin 是互動式指令只能 Stan 自己跑）。裝完驗證：`/codex:setup` 能跑通。
- 2026-06-24 建立，含 11 個重疊領域分流表 + 新-skill 重評流程。
- 2026-06-24 納入 mattpocock 6 個 cherry-pick：to-prd/to-issues/triage 進「規劃」列（issue 驅動時用）；domain-modeling / prototype / setup-pre-commit 進「各管各的」。整套 mattpocock 未裝（會變第三套通用流程、撞 superpowers/addy）。
- 2026-06-24 採用 `impeccable`(pbakaus, 41k★, Apache-2.0, frontend-design 的進化版)當**前端設計主力**：硬規則(對比/行長/clamp/OKLCH/z-index)、absolute bans(side-stripe/漸層字/玻璃/hero-metric/card grid/eyebrow/編號)、兩階 AI-slop test、23 指令、44 條 no-LLM 偵測。frontend-design 降次選(~90% 被涵蓋)。frontend-ui-engineering(元件工程)+shadcn 仍各司其職。**註**:impeccable 全威力是 per-project `npx impeccable install` + `/impeccable init`(寫 PRODUCT.md/DESIGN.md)+偵測器 CLI/瀏覽器外掛;全域 plugin 給的是設計指南+23 指令(常駐 ~316 tok)。critique/audit 也適合放 Codex 當設計審查。
- 2026-06-29 評估 + 裝 `graphify`(safishamsi，73.9k★ 但僅 3 個月新、open-core+商業版 Penpax)：**不是純 skill** 是 Python 程式(`graphifyy` 套件)＝三面(CLI / skill / MCP `graphify-mcp`)；用 `uv tool install` 裝 CLI(套件名 `graphifyy`≠`graphify` 被 supply-chain guard 擋兩次，bypass mode 才過；README 證實是官方暫用名)。實測 colonist 5 個 JS→257 節點/585 邊/14 社群、純 AST 本機免費(content.js degree 181=god file、board.js 自成一格低耦合、Community 0 的 accrue/apply/audit 最鬆=拆分接縫)；`explain` 好用，`affected`/`path` 在動態 JS 弱(要語義層+key)。**結論：對 Stan 多為小專案+已有 agent 直讀→非必需，僅理解大型/陌生 repo 時臨時用，不深接**。demo 留在 `Projects/graphify-colonist-demo/graph.html`。已加進「各管各的」。
- 2026-06-29 mineru vs mistral-ocr-tts A/B 實測（一份 10 頁中文財經論文，3 獨立審查皆判 depends）：修正「技術 PDF 一律 mineru」的過度概括→改依需求分流。mineru 勝：圖檔切出(9 張 jpg vs mistral 死連結)、形近字逐字正確、去頁碼雜訊、複雜表格 HTML；mistral 勝：中文排版乾淨(無黏字/跳脫)、保留表格「—」、inline 數學位置對、快。公式→LaTeX 此份沒觸發(無顯示型公式)。兩邊都要輕度後處理。
- 2026-06-28 安裝 `mineru`（OpenDataLab，本機 PDF→Markdown，個人 skill 自帶 venv）：非 plugin/非 skills-CLI 套件，是 pip 套件→用 uv 抓獨立 CPython 3.12（系統 3.13 在 Windows 跑不動 MinerU/ray）+ 建 venv 於 `~/.claude/skills/mineru/.venv` + 裝 `mineru[pipeline]`（CPU 正解，不含 vllm/lmdeploy）。強項＝公式→LaTeX、複雜表格、多欄閱讀順序、109 語 OCR；與 `mistral-ocr-tts`(雲端快/圖/TTS) 互補不取代→OCR/文件行改三方分流。GTX 1060 是 Pascal 用不到 GPU 故 CPU 模式慢。
- 2026-06-28 安裝 cloudflare `security-audit`（CC + Codex，global、--copy 避 Windows symlink 空殼；`-s` 只抓 SKILL.md，手動補回 4 份分階段 .md + report-schema.json + validate-findings.cjs 才完整）。實讀後判定與既有安全線「路數不同、互補不取代」：它＝多階段 agentic 獵洞 + 對抗式驗證 + 機器可讀報告（最適合真的 audit 一個 codebase、找可利用漏洞），故進「安全」列當深度稽核子情境；static-analysis＝掃描器、security-and-hardening＝設計期。Baidu Unlimited-OCR 評估後不裝：非 skill，是要 NVIDIA GPU(CUDA 12.9/≥24GB)的自架模型，Stan 的 GTX 1060 6GB 跑不動，OCR 續用 `mistral-ocr-tts`。
- 2026-06-24 (Stan 質疑「重疊≠較差」後重比品質) 再採用 4 個 mattpocock：`grilling`(逼問設計樹，補你沒有的 slot) 進新「grill」列；`diagnosing-bugs`(硬/flaky/perf bug 核心更強) 進「除錯」列；`codebase-design`(模組深度/seam) 進新列；`writing-great-skills`(skill 文筆) 進新「寫 skill」列。另：TDD 列補 vertical-slice 一句(取自 matt tdd)。待辦(harvest 非安裝)：把 matt handoff 的 redaction/DRY/suggested-skills 併進個人 handoff；把 matt git-guardrails 的 `git checkout .`/`git restore .` 兩條 regex 加進 careful_guard.py。
- 2026-06-29 評估三個 subagent 集合 repo（Boris「五原型」討論後）：wshobson/agents(37k★ MIT 天天更新)、contains-studio/agents(12k★ 無license 停更)、rshah515/claude-code-subagents(61★ MIT 停更)。**結論：三個都不安裝、不進分流表**（理由非「重疊故跳過」見 [[feedback_overlap_not_worse]]，而是：①它們是 agent 集合，Stan 自訂的 11 個 agent 已把 Builder/Sweeper/Maintainer 覆蓋到更貼自己的程度；②整批裝 100-168 個 agent 會脹 context/routing；③contains-studio 無 license 不能 vendor）。真正價值＝設計藍圖，只在「要補 Stan 唯二缺口（Grower、Prototyper scaffolder）」時去 contains-studio 挖 growth-hacker/rapid-prototyper 定義當底（wshobson 的 agent-teams 是執行期協作參考）。技術限制：subagent 不能用 AskUserQuestion/plan mode→會反問你的 Prototyper/Grower 要做成 skill 或 `claude --agent`，不做 fire-and-forget subagent。完整 setup 研究見記憶 [[reference_boris_cc_setup]]。
- 2026-06-29 深評 8 個 repo subagent（fetch 原檔→比品質→對抗式複查 workflow，24 agent 全 opus），只有 1 個過：contains-studio `growth-hacker`→填 Grower 真缺口（你整套 stack 完全沒碰上線後留存/啟用/漏斗/A-B/上架優化）。因無授權(不能抄)+Grower 要反問用戶→自寫成 main-session skill `product-growth`(organic/product-led scoped，AARRR+ICE，已加進「各管各的」)。其餘 7 全 skip/harvest：test-automator(撞 test-first 鐵律、被 agent-skills:TDD 蓋)、debugger(diagnosing-bugs 子集、缺紅燈閘、sonnet)、deployment-engineer(k8s 企業級過重、ci-cd-and-automation 已蓋)、legacy-modernizer(deprecation-and-migration 已蓋)、backend-architect(自有更貼身+撞名)、architect-review(harvest 影響度 High/Med/Low triage 進 system-architect)、rapid-prototyper(harvest 進 guided-dev；scaffolding 要反問故不宜 subagent)。多數還 pin sonnet/haiku 違 opus 規則。印證 [[feedback_overlap_not_worse]]：實讀後 7 個重疊的真的沒贏、1 個不重疊的才值得收。
- 2026-06-29 拆分職責：新建 `new-skill`（標題 New Skill）接手「新工具該不該納入」的整套評估流程（查重複→實讀→比品質→對抗式複查→分類→寫回），分類規則也搬過去。本檔（skill-routing）瘦身成只維護路由表本身，原「維護」段改為指向 new-skill 的指標。自動程度＝安全的寫回自動做、破壞性的（取代/停用/改 settings.json/裝 plugin/複製檔）先問；只在使用者明講評估意圖+給網址時觸發。
- 2026-07-05 橫向評比六大網頁設計 skill（源自 josh.hu/designcompare 復現 @vista8 測評：同題儀表板、獨立 agent 生成、7 變體；本次 5 實讀 + 3 對抗反辯 agent 全 opus 實抓原檔）：**四個候選全不裝，`impeccable` 維持前端設計唯一主力**。①ui-ux-pro-max(MIT,101k★,142KB CSV 庫+python 檢索)=實測評最差「模板反限制發揮」、偏 app UI 規範、強依賴 python3→不收。②web-design-guidelines(Vercel,30 行薄抓取器+~80 條 MUST/NEVER)=被 impeccable audit.md(完整 A11y 節)/interaction-design.md(明禁 outline:none)/harden.md(axe/WAVE)蓋掉；每次執行 WebFetch GitHub raw 單點、抓不到會靜默產「零違規」假綠燈；87 條中 18 條英文品牌 copywriting(Title Case/彎引號)對繁中專案是負值；包裝層 repo 無 LICENSE→不收。③emil-design-eng(Emil Kowalski 真身,MIT,27KB)=**實質是動效工程 skill 非灰階視覺**；impeccable 的 animate.md(時長分級/easing 白黑名單/只動 transform+opacity/reduced-motion)已蓋 ~90%，真增量僅 2 條；且首輪觸發回 canned 課程廣告→不裝，harvest 2 條。④taste-skill(Leonxlnx,56k★,MIT,1950 行)=實測「AI 味最小」但 impeccable 的 detect-text.mjs(568 行 regex 零 LLM)已蓋其黑名單大部分(overused-font 字型黑名單比它長、em-dash-overuse、filler 動詞全中)；致命傷=designcompare 實測場景(dashboard)正落在 taste 自陳「NOT for: Dashboards」範圍、證據與 landing 路由不對口；v2 掛 experimental→不裝，harvest 3 條。**harvest 5 條差集**（emil：頻率閘門「日觸發 100+ 次/鍵盤發起操作永不動畫」+「進場禁 scale(0) 改 scale(0.95)+opacity」；taste：禁 Jane/John Doe 假名假頭像 + 禁 99.99% 假完美數字 + 禁 beige/brass/oxblood premium-consumer 配色輪替）→ **已落地（Stan 拍板 A 案）**：直接補進 plugin cache 3.8.0 的 animate.md（NEVER 清單 +2）與 SKILL.md（Absolute bans +2，Jane Doe 與假數字合併一條），檔內有 `Stan overlay 2026-07-05` 註解可 grep；plugin 升版會蓋掉，重貼指引見 memory [[project-impeccable-overlay]]。方法教訓：評 skill 能力要讀它的知識庫（impeccable 有 animate.md），不能拿單次產出數 keyframes 當證據。
- 2026-07-03 評估 `akseolabs-seo/fable-soul`（26★，**當天剛建**零時間考驗，MIT）：Fable 判斷層蒸餾 skill（soul.md 19 條+藉口表+red flags、RED-GREEN capture loop、sync 鏡像機制）。安全審查：3 個 script 全乾淨（check_update 純唯讀、sync_soul 有防覆寫守衛、無外傳），但 check_update=持續上游信任通道。**結論：不安裝**（內容與 CC 系統提示/superpowers/進行中的制度檔案集大量重疊；sync-to-CLAUDE.md 模式不適合 Stan 的既有配置；避免上游信任+雙源分歧）；**harvest 5 個真缺口設計進 skill-distillation Checkpoint 1.5**（MIT 合法）：①Rationalizations 藉口逐字表（比抽象規則好觸發，直擊審計「規則存在但沒執行」痛點）②RED-GREEN capture loop（prompt 規則改動先重現失敗再改，no untested rule edits）③Growth Budget（250 行預算防規則通膨）④規則五要素準入（failure mode/trigger/changed behavior/proof surface/strip condition）⑤Mode Selector（簡單問題直答防流程表演）。transfer-prompts 六模板與我們考古/審計/三輪 review 同構、無新增價值。
